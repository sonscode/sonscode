export class User {
    constructor(
        public name: string,
        public email: string,
        public phone: number,
        public message: string,
        public subscribe: boolean
    ) {}
}
